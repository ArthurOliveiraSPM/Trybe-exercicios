// array.metodo(parametros);

const myArray = [1, 2, 3];
myArray.push(4);

console.log(myArray); // [1, 2, 3, 4]

//Também é possível que a HOF receba como parâmetro uma função callback. 
array.metodo(() => {});